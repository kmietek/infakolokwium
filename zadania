Zad 1: 
#include <iostream>

using namespace std;
int temp;

int main()
{
cout << "podaj temperature: " << endl;
cin >> temp;
if (temp<21)
    cout << "Grzanie" << endl;
    else if (temp>21)
        cout << "Chlodzenie" << endl;
    else cout << "Optymalna" << endl;
return 0;
}

============================================================
Zad2:
#include <iostream>
#include <cmath>

using namespace std;

int a,suma;
double pierwiastek;
int main()
{
do
{
cin >> a;
suma = suma+a;
} while (a!=0);
cout << "suma = " << suma << endl;
pierwiastek=sqrt(suma);
cout << "pierwiastek = " << pierwiastek << endl;
return 0;
}

==============================================================
Zad3:
#include <iostream>
#include <string>

using namespace std;

string T[5];
int main()
{
    cout << "Wprowadz dane do tabeli:" << endl;
    for (char i=0;i<5;i++)
    {
    cin >> T[i];
    }
    cout << "Liczba znakow 1: " << T[0].length() << endl;
    cout << "Liczba znakow 2: " << T[1].length() << endl;
    cout << "Liczba znakow 5: " << T[4].length() << endl;
return 0;
}

=================================================================
Zad4:
#include <iostream>
#include <string>

using namespace std;


int main()
{
int x;
cout << "Podaj ilosc kolumn: " << endl;
cin >> x;
string T[x];
    for(int i=0;i<x;i++)
    {
        cin >> T[i];
    }
    cout << endl;
    for(int i=(x-1);i>-1;i--)
    {
        cout << T[i] << endl;
    }
return 0;
}
